language: node_js
install:
    - cd $TRAVIS_BUILD_DIR
    - npm install
deploy:
    provider: pages
    cleanup: true
    token: $GITHUB_TOKEN
    keep_history: false
    local_dir: $TRAVIS_BUILD_DIR
    on:
        branch: main
    target_branch: gh-pages
